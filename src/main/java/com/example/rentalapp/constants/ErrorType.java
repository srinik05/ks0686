package com.example.rentalapp.constants;

/**
 * Enum representing different types of errors that can occur in the application.
 */
public enum ErrorType {
    RENTALDAYS_INVALID,
    DISCOUNTPERCENT_INVALID,
    TOOL_NOT_FOUND,
    CHECKOUTDATE_INVALID
}
